---
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="logo">
        <h1 class="brand-name">
          <span class="text-neon-green">Zona</span>
          <span class="text-neon-pink">Fría</span>
        </h1>
        <div class="logo-decoration"></div>
      </div>
      
      <p class="hero-subtitle">
        Helados <span class="text-neon-blue">Naturales</span> • Milkshakes • Frutas Frescas • Panes para Lonch
      </p>
      
      <p class="hero-description">
        Descubre el paraíso de los sabores naturales. En Zona Fría creamos experiencias únicas 
        con ingredientes frescos y recetas artesanales que despiertan tus sentidos.
      </p>
      
      <div class="hero-actions">
        <a href="#ubicaciones" class="btn btn-primary">
          Ver Ubicaciones
        </a>
        <a href="#productos" class="btn btn-secondary">
          Nuestros Productos
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
    padding: 80px 0 60px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }
  
  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(57, 255, 20, 0.05) 0%, transparent 70%);
    animation: float 20s ease-in-out infinite;
  }
  
  .hero-content {
    position: relative;
    z-index: 2;
  }
  
  .logo {
    position: relative;
    margin-bottom: 30px;
  }
  
  .brand-name {
    font-size: 4rem;
    font-weight: 700;
    margin: 0;
    letter-spacing: -2px;
    text-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }
  
  .logo-decoration {
    width: 100px;
    height: 4px;
    background: linear-gradient(90deg, var(--neon-green), var(--neon-pink), var(--neon-blue));
    margin: 10px auto;
    border-radius: 2px;
  }
  
  .hero-subtitle {
    font-size: 1.5rem;
    font-weight: 600;
    margin: 20px 0;
    color: var(--dark);
  }
  
  .hero-description {
    font-size: 1.2rem;
    color: #666;
    max-width: 600px;
    margin: 30px auto;
    line-height: 1.7;
  }
  
  .hero-actions {
    margin-top: 40px;
    display: flex;
    gap: 20px;
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .btn {
    display: inline-block;
    padding: 15px 30px;
    text-decoration: none;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1.1rem;
    transition: all 0.3s ease;
    text-transform: uppercase;
    letter-spacing: 1px;
    position: relative;
    overflow: hidden;
  }
  
  .btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s;
  }
  
  .btn:hover::before {
    left: 100%;
  }
  
  .btn-primary {
    background: linear-gradient(135deg, var(--neon-green), #32e010);
    color: white;
    box-shadow: 0 8px 25px rgba(57, 255, 20, 0.3);
  }
  
  .btn-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(57, 255, 20, 0.4);
  }
  
  .btn-secondary {
    background: linear-gradient(135deg, var(--neon-pink), #e6064d);
    color: white;
    box-shadow: 0 8px 25px rgba(255, 7, 58, 0.3);
  }
  
  .btn-secondary:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(255, 7, 58, 0.4);
  }
  
  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(5deg); }
  }
  
  /* Responsive */
  @media (max-width: 768px) {
    .hero {
      padding: 60px 0 40px;
    }
    
    .brand-name {
      font-size: 2.5rem;
    }
    
    .hero-subtitle {
      font-size: 1.2rem;
    }
    
    .hero-description {
      font-size: 1rem;
      padding: 0 10px;
    }
    
    .hero-actions {
      flex-direction: column;
      align-items: center;
    }
    
    .btn {
      width: 250px;
      padding: 12px 25px;
      font-size: 1rem;
    }
  }
</style>
